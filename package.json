{
  "name": "abstract-xp-tracker",
  "version": "1.0.0",
  "description": "Abstract XP Tracker - Full Stack Application",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=16.0.0"
  }
}
